<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mralonso.android.presentation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/app_theme">

    <activity android:name="com.mralonso.android.presentation.activities.BooksActivity">

        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>

    </activity>

    <activity android:name="com.mralonso.android.presentation.activities.BookDetailActivity"/>

    </application>

</manifest>
